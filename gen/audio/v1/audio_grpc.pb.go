// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: audio/v1/audio.proto

package audiov1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AudioService_Probe_FullMethodName             = "/audio.v1.AudioService/Probe"
	AudioService_Normalize_FullMethodName         = "/audio.v1.AudioService/Normalize"
	AudioService_TranscribeSegment_FullMethodName = "/audio.v1.AudioService/TranscribeSegment"
)

// AudioServiceClient is the client API for AudioService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// AudioService transcodes and transcribes an audio asset (CON-282). It is the
// media sibling of video-service (CON-148): stateless, internal-only, reached by
// the Ogen API over the Railway private network, and handed short-lived
// presigned URLs so the (potentially hour-long, multi-hundred-MB) audio never
// streams through the API process. Unlike the one-shot pdf/video/documents
// services, real audio is long-running, so ogen — not this service — owns the
// resumable state machine: the service exposes three narrow, independently
// retryable compute RPCs and holds NO state and NO database.
//
// Transcription runs on Gemini multimodal via the Gemini Developer API (the same
// gemini_api_key the embedder uses); per-utterance offsets/confidence are
// model-reported and approximate (the accepted v1 trade-off — no dedicated ASR).
// Implemented by the ogen-app/audio-service repo; the Ogen API consumes a
// versioned, pinned client generated from this contract.
type AudioServiceClient interface {
	// Probe range-reads the source (presigned GET) and returns duration /
	// channels / sample-rate + a silence/zero-length verdict, without
	// transcoding. Cheap; drives validation and ogen's pre-processing tier gate.
	Probe(ctx context.Context, in *ProbeRequest, opts ...grpc.CallOption) (*ProbeResponse, error)
	// Normalize transcodes the source (presigned GET) to mono @
	// target_sample_rate and streams the result to dest_put_url (presigned PUT).
	// Never buffers the whole file (ffmpeg pipes). Returns the derivative's
	// metadata. The canonical derivative (mono 16 kHz Opus) is reused by every
	// TranscribeSegment call and persisted once by ogen (evicted with the asset).
	Normalize(ctx context.Context, in *NormalizeRequest, opts ...grpc.CallOption) (*NormalizeResponse, error)
	// TranscribeSegment cuts [start_ms, end_ms) from the normalized derivative
	// (presigned GET, ffmpeg range-read) and transcribes it against the Gemini
	// multimodal backend, returning utterances whose offsets are rebased to the
	// ORIGINAL asset timeline. Independently retryable per segment: ogen
	// checkpoints each segment and resumes from the first incomplete one.
	TranscribeSegment(ctx context.Context, in *TranscribeSegmentRequest, opts ...grpc.CallOption) (*TranscribeSegmentResponse, error)
}

type audioServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewAudioServiceClient(cc grpc.ClientConnInterface) AudioServiceClient {
	return &audioServiceClient{cc}
}

func (c *audioServiceClient) Probe(ctx context.Context, in *ProbeRequest, opts ...grpc.CallOption) (*ProbeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ProbeResponse)
	err := c.cc.Invoke(ctx, AudioService_Probe_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *audioServiceClient) Normalize(ctx context.Context, in *NormalizeRequest, opts ...grpc.CallOption) (*NormalizeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(NormalizeResponse)
	err := c.cc.Invoke(ctx, AudioService_Normalize_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *audioServiceClient) TranscribeSegment(ctx context.Context, in *TranscribeSegmentRequest, opts ...grpc.CallOption) (*TranscribeSegmentResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(TranscribeSegmentResponse)
	err := c.cc.Invoke(ctx, AudioService_TranscribeSegment_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AudioServiceServer is the server API for AudioService service.
// All implementations must embed UnimplementedAudioServiceServer
// for forward compatibility.
//
// AudioService transcodes and transcribes an audio asset (CON-282). It is the
// media sibling of video-service (CON-148): stateless, internal-only, reached by
// the Ogen API over the Railway private network, and handed short-lived
// presigned URLs so the (potentially hour-long, multi-hundred-MB) audio never
// streams through the API process. Unlike the one-shot pdf/video/documents
// services, real audio is long-running, so ogen — not this service — owns the
// resumable state machine: the service exposes three narrow, independently
// retryable compute RPCs and holds NO state and NO database.
//
// Transcription runs on Gemini multimodal via the Gemini Developer API (the same
// gemini_api_key the embedder uses); per-utterance offsets/confidence are
// model-reported and approximate (the accepted v1 trade-off — no dedicated ASR).
// Implemented by the ogen-app/audio-service repo; the Ogen API consumes a
// versioned, pinned client generated from this contract.
type AudioServiceServer interface {
	// Probe range-reads the source (presigned GET) and returns duration /
	// channels / sample-rate + a silence/zero-length verdict, without
	// transcoding. Cheap; drives validation and ogen's pre-processing tier gate.
	Probe(context.Context, *ProbeRequest) (*ProbeResponse, error)
	// Normalize transcodes the source (presigned GET) to mono @
	// target_sample_rate and streams the result to dest_put_url (presigned PUT).
	// Never buffers the whole file (ffmpeg pipes). Returns the derivative's
	// metadata. The canonical derivative (mono 16 kHz Opus) is reused by every
	// TranscribeSegment call and persisted once by ogen (evicted with the asset).
	Normalize(context.Context, *NormalizeRequest) (*NormalizeResponse, error)
	// TranscribeSegment cuts [start_ms, end_ms) from the normalized derivative
	// (presigned GET, ffmpeg range-read) and transcribes it against the Gemini
	// multimodal backend, returning utterances whose offsets are rebased to the
	// ORIGINAL asset timeline. Independently retryable per segment: ogen
	// checkpoints each segment and resumes from the first incomplete one.
	TranscribeSegment(context.Context, *TranscribeSegmentRequest) (*TranscribeSegmentResponse, error)
	mustEmbedUnimplementedAudioServiceServer()
}

// UnimplementedAudioServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAudioServiceServer struct{}

func (UnimplementedAudioServiceServer) Probe(context.Context, *ProbeRequest) (*ProbeResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Probe not implemented")
}
func (UnimplementedAudioServiceServer) Normalize(context.Context, *NormalizeRequest) (*NormalizeResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Normalize not implemented")
}
func (UnimplementedAudioServiceServer) TranscribeSegment(context.Context, *TranscribeSegmentRequest) (*TranscribeSegmentResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method TranscribeSegment not implemented")
}
func (UnimplementedAudioServiceServer) mustEmbedUnimplementedAudioServiceServer() {}
func (UnimplementedAudioServiceServer) testEmbeddedByValue()                      {}

// UnsafeAudioServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AudioServiceServer will
// result in compilation errors.
type UnsafeAudioServiceServer interface {
	mustEmbedUnimplementedAudioServiceServer()
}

func RegisterAudioServiceServer(s grpc.ServiceRegistrar, srv AudioServiceServer) {
	// If the following call panics, it indicates UnimplementedAudioServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AudioService_ServiceDesc, srv)
}

func _AudioService_Probe_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ProbeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AudioServiceServer).Probe(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AudioService_Probe_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AudioServiceServer).Probe(ctx, req.(*ProbeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AudioService_Normalize_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(NormalizeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AudioServiceServer).Normalize(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AudioService_Normalize_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AudioServiceServer).Normalize(ctx, req.(*NormalizeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AudioService_TranscribeSegment_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(TranscribeSegmentRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AudioServiceServer).TranscribeSegment(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AudioService_TranscribeSegment_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AudioServiceServer).TranscribeSegment(ctx, req.(*TranscribeSegmentRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AudioService_ServiceDesc is the grpc.ServiceDesc for AudioService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AudioService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "audio.v1.AudioService",
	HandlerType: (*AudioServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Probe",
			Handler:    _AudioService_Probe_Handler,
		},
		{
			MethodName: "Normalize",
			Handler:    _AudioService_Normalize_Handler,
		},
		{
			MethodName: "TranscribeSegment",
			Handler:    _AudioService_TranscribeSegment_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "audio/v1/audio.proto",
}
