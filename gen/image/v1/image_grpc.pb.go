// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: image/v1/image.proto

package imagev1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ImageService_Extract_FullMethodName           = "/image.v1.ImageService/Extract"
	ImageService_PrepareAttachment_FullMethodName = "/image.v1.ImageService/PrepareAttachment"
	ImageService_GenerateAltText_FullMethodName   = "/image.v1.ImageService/GenerateAltText"
)

// ImageServiceClient is the client API for ImageService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ImageService is the single authority for every image in the platform (CON-281):
// validation/sniffing, dimensions + animation detection, EXIF-strip/normalize,
// alt-text generation, and — for content-bank assets — Gemini vision
// classification/extraction/description. It is the media sibling of audio-service
// (CON-282) and the third ingestion path beside pdf-service and documents-service:
// stateless, internal-only, reached by the Ogen API over the Railway private
// network, and handed short-lived presigned URLs so image bytes never stream
// through the API process.
//
// Unlike audio (normalize-only compute), image-service is a FULL AI service: the
// vision credential, per-shape structured prompts, media_resolution, and the
// one-shot escalation all live IN the service. It runs on Gemini multimodal via
// the Gemini Developer API (the same gemini_api_key the embedder uses) and
// returns per-call token usage so ogen prices it (CON-86); EU-residency is
// deferred to a future Vertex europe-west swap (shared with audio/CON-105).
// Implemented by the ogen-app/image-service repo; the Ogen API consumes a
// versioned, pinned client generated from this contract.
type ImageServiceClient interface {
	// Extract is the full content-bank pipeline: normalize (re-encode/downscale) +
	// classify + shape-specific structured extraction + description + alt text,
	// escalating once on low confidence. Reads the original (presigned GET), writes
	// the normalized derivative (presigned PUT), and returns Blocks + artifacts +
	// metadata + usage.
	Extract(ctx context.Context, in *ExtractRequest, opts ...grpc.CallOption) (*ExtractResponse, error)
	// PrepareAttachment is the light post-attachment path: validate + metadata
	// (mime/dims/animated/checksum) + EXIF-strip with PIXELS PRESERVED (no lossy
	// re-encode / no downscale) + optional alt text. Reads the uploaded original
	// (presigned GET) and writes the cleaned, metadata-free copy (presigned PUT).
	// No vision extraction — it's a publishing artifact, not content.
	PrepareAttachment(ctx context.Context, in *PrepareAttachmentRequest, opts ...grpc.CallOption) (*PrepareAttachmentResponse, error)
	// GenerateAltText re-runs ONLY alt-text generation for an already-stored image.
	GenerateAltText(ctx context.Context, in *GenerateAltTextRequest, opts ...grpc.CallOption) (*GenerateAltTextResponse, error)
}

type imageServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewImageServiceClient(cc grpc.ClientConnInterface) ImageServiceClient {
	return &imageServiceClient{cc}
}

func (c *imageServiceClient) Extract(ctx context.Context, in *ExtractRequest, opts ...grpc.CallOption) (*ExtractResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ExtractResponse)
	err := c.cc.Invoke(ctx, ImageService_Extract_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *imageServiceClient) PrepareAttachment(ctx context.Context, in *PrepareAttachmentRequest, opts ...grpc.CallOption) (*PrepareAttachmentResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(PrepareAttachmentResponse)
	err := c.cc.Invoke(ctx, ImageService_PrepareAttachment_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *imageServiceClient) GenerateAltText(ctx context.Context, in *GenerateAltTextRequest, opts ...grpc.CallOption) (*GenerateAltTextResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GenerateAltTextResponse)
	err := c.cc.Invoke(ctx, ImageService_GenerateAltText_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ImageServiceServer is the server API for ImageService service.
// All implementations must embed UnimplementedImageServiceServer
// for forward compatibility.
//
// ImageService is the single authority for every image in the platform (CON-281):
// validation/sniffing, dimensions + animation detection, EXIF-strip/normalize,
// alt-text generation, and — for content-bank assets — Gemini vision
// classification/extraction/description. It is the media sibling of audio-service
// (CON-282) and the third ingestion path beside pdf-service and documents-service:
// stateless, internal-only, reached by the Ogen API over the Railway private
// network, and handed short-lived presigned URLs so image bytes never stream
// through the API process.
//
// Unlike audio (normalize-only compute), image-service is a FULL AI service: the
// vision credential, per-shape structured prompts, media_resolution, and the
// one-shot escalation all live IN the service. It runs on Gemini multimodal via
// the Gemini Developer API (the same gemini_api_key the embedder uses) and
// returns per-call token usage so ogen prices it (CON-86); EU-residency is
// deferred to a future Vertex europe-west swap (shared with audio/CON-105).
// Implemented by the ogen-app/image-service repo; the Ogen API consumes a
// versioned, pinned client generated from this contract.
type ImageServiceServer interface {
	// Extract is the full content-bank pipeline: normalize (re-encode/downscale) +
	// classify + shape-specific structured extraction + description + alt text,
	// escalating once on low confidence. Reads the original (presigned GET), writes
	// the normalized derivative (presigned PUT), and returns Blocks + artifacts +
	// metadata + usage.
	Extract(context.Context, *ExtractRequest) (*ExtractResponse, error)
	// PrepareAttachment is the light post-attachment path: validate + metadata
	// (mime/dims/animated/checksum) + EXIF-strip with PIXELS PRESERVED (no lossy
	// re-encode / no downscale) + optional alt text. Reads the uploaded original
	// (presigned GET) and writes the cleaned, metadata-free copy (presigned PUT).
	// No vision extraction — it's a publishing artifact, not content.
	PrepareAttachment(context.Context, *PrepareAttachmentRequest) (*PrepareAttachmentResponse, error)
	// GenerateAltText re-runs ONLY alt-text generation for an already-stored image.
	GenerateAltText(context.Context, *GenerateAltTextRequest) (*GenerateAltTextResponse, error)
	mustEmbedUnimplementedImageServiceServer()
}

// UnimplementedImageServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedImageServiceServer struct{}

func (UnimplementedImageServiceServer) Extract(context.Context, *ExtractRequest) (*ExtractResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Extract not implemented")
}
func (UnimplementedImageServiceServer) PrepareAttachment(context.Context, *PrepareAttachmentRequest) (*PrepareAttachmentResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method PrepareAttachment not implemented")
}
func (UnimplementedImageServiceServer) GenerateAltText(context.Context, *GenerateAltTextRequest) (*GenerateAltTextResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GenerateAltText not implemented")
}
func (UnimplementedImageServiceServer) mustEmbedUnimplementedImageServiceServer() {}
func (UnimplementedImageServiceServer) testEmbeddedByValue()                      {}

// UnsafeImageServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ImageServiceServer will
// result in compilation errors.
type UnsafeImageServiceServer interface {
	mustEmbedUnimplementedImageServiceServer()
}

func RegisterImageServiceServer(s grpc.ServiceRegistrar, srv ImageServiceServer) {
	// If the following call panics, it indicates UnimplementedImageServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ImageService_ServiceDesc, srv)
}

func _ImageService_Extract_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ExtractRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ImageServiceServer).Extract(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ImageService_Extract_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ImageServiceServer).Extract(ctx, req.(*ExtractRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ImageService_PrepareAttachment_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(PrepareAttachmentRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ImageServiceServer).PrepareAttachment(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ImageService_PrepareAttachment_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ImageServiceServer).PrepareAttachment(ctx, req.(*PrepareAttachmentRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ImageService_GenerateAltText_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GenerateAltTextRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ImageServiceServer).GenerateAltText(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ImageService_GenerateAltText_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ImageServiceServer).GenerateAltText(ctx, req.(*GenerateAltTextRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// ImageService_ServiceDesc is the grpc.ServiceDesc for ImageService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ImageService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "image.v1.ImageService",
	HandlerType: (*ImageServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Extract",
			Handler:    _ImageService_Extract_Handler,
		},
		{
			MethodName: "PrepareAttachment",
			Handler:    _ImageService_PrepareAttachment_Handler,
		},
		{
			MethodName: "GenerateAltText",
			Handler:    _ImageService_GenerateAltText_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "image/v1/image.proto",
}
